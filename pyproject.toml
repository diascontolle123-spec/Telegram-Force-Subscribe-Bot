[project]
name = "telegram-force-subscribe-bot"
version = "0.1.0"
description = "Telegram bot with mandatory channel subscription and gated key access"
requires-python = ">=3.13"
dependencies = [
    "flask>=3.0,<4.0",
    "python-telegram-bot==22.8",
    "telegram>=0.0.1",
]
